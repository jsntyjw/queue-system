import React from "react";
import { INavResources } from "./types";
interface IProps {
    resources: INavResources;
    compress?: boolean;
    onClick?: (event: React.MouseEvent<HTMLAnchorElement>) => void;
    "data-testid"?: string;
}
export declare const Brand: ({ resources, compress, onClick, ...otherProps }: IProps) => JSX.Element;
export {};
